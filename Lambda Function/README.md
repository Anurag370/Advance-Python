# Lambda Function
It is a small anonymous function without a name.
It's defined bt the lambda keyword and has the following syntax
    
    lambda arguments : expression

It's norammly used when small functions are needed
to use as arguments to higher-order function.